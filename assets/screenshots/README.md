# Screenshots

Drop PNGs here matching the names below; the READMEs reference them by name.
Every image should be a real capture from the dsh web profile (dark theme).

| File | What to capture | Notes |
| --- | --- | --- |
| `rewind-button.png` | A **user message** row with the **↶ rewind button** visible in its action row (hover state) | Take the shot while hovering; the button sits next to the existing copy / "branch in new chat" icons on the right of a user message |
| `mode-popover.png` | The **mode-selection popover** after clicking ↶ | Shows the target line (`seq N · HH:MM · preview`) plus the two options — "Rewind conversation only" and "Rewind conversation and code" — and Cancel. Ideally capture with the "conversation and code" option enabled (its hint may read "Checking for file changes…" or the final state) |
| `impact-list.png` | The **impact list** shown after choosing "Rewind conversation and code" | A session that has made tracked file edits after the target, so the list shows at least one file to restore / delete; include the "Back" and "Confirm rewind" buttons |
| `guard-hint.png` | The **manual `/rewind` guard hint** | Type `/rewind @12 both` into the composer and press Enter; capture the transient hint ("…use the ↶ button on a message to rewind") that appears above the composer card |
